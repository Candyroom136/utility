const now = new Date(2022, -1, 0)
const nowDate = now.getDate()
const nowMonth = now.getMonth()
const nowYear = now.getFullYear()
const nowDay = now.getDay()

console.log( nowYear, nowMonth,nowDate, nowDay)

